#include <iostream>
#include "include/functors.h"

/*
 * A) Write a function add which takes two arguments
 *      of the same type T and returns type T
 */

/*
 * B) Write two classes (Increase and Decrease) using the Funktor
 *      Class and a function apply() which takes one of them as first
 *      argument and as a second (int or float) which this is applied to
 */



int main() {
    // Call the function add with integers and floats
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
